# Web Framework
# Security (OWASP A06): floors raised to the patched baseline currently deployed.
# Starlette is pinned explicitly so it cannot resolve below the FileResponse
# Range-header DoS fix (CVE-2025-62727, fixed 0.49.1). Use a lockfile + pip-audit
# in CI for full reproducibility.
fastapi>=0.137.1
starlette>=1.3.1
uvicorn[standard]>=0.49.0
pydantic[email]>=2.4.0
email-validator>=2.0.0

# Core AI/ML Dependencies (using prebuilt wheels)
transformers>=4.35.0
torch>=2.0.0
spacy>=3.5.0
nltk>=3.9
scikit-learn>=1.3.0
numpy>=1.24.0

# Supabase Integration
supabase>=2.0.0
psycopg2-binary>=2.9.0

# Data Processing
requests>=2.32.2
feedparser>=6.0.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
python-dateutil>=2.8.0
python-dotenv>=1.0.0

# Background Processing (Celery + Redis)
celery>=5.3.0
redis>=5.0.0

# Geospatial
geopy>=2.4.0

# Rate Limiting & Security
slowapi>=0.1.9

# AWS Services (SNS for SMS notifications)
boto3>=1.26.0
botocore>=1.29.0

# Field-Level Encryption (PII Protection - RA 10173 Compliance)
cryptography>=46.0.5

# Server-Sent Events (SSE)
sse-starlette>=1.6.0

# PDF Generation & Image Processing
reportlab>=4.0.0
pillow>=10.0.0
python-multipart>=0.0.32

# Testing
pytest>=9.0.3
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# Code Quality
black>=23.11.0
flake8>=6.1.0
urllib3>=2.6.3 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
dnspython>=2.6.1 # not directly required, pinned by Snyk to avoid a vulnerability
