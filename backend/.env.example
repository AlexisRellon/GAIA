# Supabase Configuration
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database Configuration (PostGIS)
DATABASE_URL=postgresql://postgres:password@localhost:54322/postgres

# AI Model Configuration
CLIMATE_NLI_MODEL_PATH=models/climate-nli
GEO_NER_MODEL_PATH=models/geo-ner

# RSS Feed Sources
RSS_FEEDS=https://www.gmanetwork.com/news/rss/news/,https://newsinfo.inquirer.net/feed

# API Configuration
API_PORT=8000
API_HOST=0.0.0.0
ENV=development  # development | staging | production

# CORS Configuration
# Notes:
# - Set CORS_ORIGINS to a comma-separated list of allowed frontend origins.
# - You may use platform-style wildcard subdomains like `https://*.vercel.app`
#   or `https://*.up.railway.app`. Wildcard entries are converted to a
#   secure regex for Starlette's CORSMiddleware (supports subdomains only).
# - Avoid setting a single '*' in production unless you understand the impact.
# Examples:
#   # Vercel + Railway (recommended using your actual deployed front-end domain)
#   CORS_ORIGINS=https://gaia-ph.vercel.app,https://your-custom-domain.com
#   # Note: wildcard subdomains are not supported for security. Use explicit domains:
#   CORS_ORIGINS=https://gaia-ph.vercel.app,https://your-custom-domain.com
# Development (fallback):
## Development default: localhost
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

## Production recommendation (set in Railway env vars):
## Allow the Vercel frontend origin that hosts the GAIA PWA
## Note: this should be set in the Railway service environment variables
## rather than in source-controlled files.
## Example (Railway dashboard):
## CORS_ORIGINS=https://gaia-ph.vercel.app

# Railway Deployment (Production)
# CORS_ORIGINS=https://*.up.railway.app,https://gaia.railway.app
# RAILWAY_ENVIRONMENT=production

# Vercel + Railway Deployment (Production)
# CORS_ORIGINS=https://*.vercel.app,https://*.up.railway.app,https://your-app.vercel.app
# ENV=production

# Confidence Thresholds
MIN_CONFIDENCE_THRESHOLD=0.7
CITIZEN_REPORT_CONFIDENCE=0.3

# Geographic Boundaries
PHILIPPINES_BOUNDS_GEOJSON=data/philippines-boundaries.geojson

# reCAPTCHA
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key
